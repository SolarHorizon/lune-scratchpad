local all = require("./all")
local name = require("./name")
local scope = require("./scope")
local version = require("./version")

return {
	all = all,
	name = name,
	scope = scope,
	version = version,
}
